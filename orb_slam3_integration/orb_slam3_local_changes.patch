diff --git a/CMakeLists.txt b/CMakeLists.txt
index 016e743..ebf877a 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -14,12 +14,12 @@ set(CMAKE_CXX_FLAGS_RELEASE "${CMAKE_CXX_FLAGS_RELEASE} -march=native")
 
 # Check C++11 or C++0x support
 include(CheckCXXCompilerFlag)
-CHECK_CXX_COMPILER_FLAG("-std=c++11" COMPILER_SUPPORTS_CXX11)
+CHECK_CXX_COMPILER_FLAG("-std=c++14" COMPILER_SUPPORTS_CXX11)
 CHECK_CXX_COMPILER_FLAG("-std=c++0x" COMPILER_SUPPORTS_CXX0X)
 if(COMPILER_SUPPORTS_CXX11)
-   set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11")
+   set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++14")
    add_definitions(-DCOMPILEDWITHC11)
-   message(STATUS "Using flag -std=c++11.")
+   message(STATUS "Using flag -std=c++14.")
 elseif(COMPILER_SUPPORTS_CXX0X)
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++0x")
    add_definitions(-DCOMPILEDWITHC0X)
@@ -125,6 +125,14 @@ ${PROJECT_SOURCE_DIR}/Thirdparty/g2o/lib/libg2o.so
 -lcrypto
 )
 
+# orb_capi: minimal C-linkage wrapper around System (Stereo-Inertial), for
+# embedding a live ORB-SLAM3 session inside another process (e.g. driven via
+# Python ctypes - see hardware_project/camera/orbslam_bridge.py). Unconditional
+# (no realsense2 dependency, unlike the Examples below) - mirrors how
+# open_vins/ov_msckf builds its own ov_capi target the same way.
+add_library(orb_capi SHARED src/orb_capi.cc include/orb_capi.h)
+target_link_libraries(orb_capi ${PROJECT_NAME})
+
 # If RealSense SDK is found the library is added and its examples compiled
 if(realsense2_FOUND)
     include_directories(${PROJECT_NAME}
diff --git a/Examples/Monocular-Inertial/mono_inertial_euroc.cc b/Examples/Monocular-Inertial/mono_inertial_euroc.cc
index b9f320f..bff32b2 100644
--- a/Examples/Monocular-Inertial/mono_inertial_euroc.cc
+++ b/Examples/Monocular-Inertial/mono_inertial_euroc.cc
@@ -117,7 +117,7 @@ int main(int argc, char *argv[])
     cout.precision(17);
 
     // Create SLAM system. It initializes all system threads and gets ready to process frames.
-    ORB_SLAM3::System SLAM(argv[1],argv[2],ORB_SLAM3::System::IMU_MONOCULAR, true);
+    ORB_SLAM3::System SLAM(argv[1],argv[2],ORB_SLAM3::System::IMU_MONOCULAR, false);
     float imageScale = SLAM.GetImageScale();
 
     double t_resize = 0.f;
diff --git a/Examples/Stereo-Inertial/stereo_inertial_realsense_D435i.cc b/Examples/Stereo-Inertial/stereo_inertial_realsense_D435i.cc
index d3a03bf..557729c 100644
--- a/Examples/Stereo-Inertial/stereo_inertial_realsense_D435i.cc
+++ b/Examples/Stereo-Inertial/stereo_inertial_realsense_D435i.cc
@@ -159,8 +159,12 @@ int main(int argc, char **argv) {
     rs2::pipeline pipe;
     // Create a configuration for configuring the pipeline with a non default profile
     rs2::config cfg;
-    cfg.enable_stream(RS2_STREAM_INFRARED, 1, 640, 480, RS2_FORMAT_Y8, 30);
-    cfg.enable_stream(RS2_STREAM_INFRARED, 2, 640, 480, RS2_FORMAT_Y8, 30);
+    // 848x480 @ 60fps to match our measured/verified RealSense_D435i_ours.yaml
+    // calibration (Camera1.fx/fy/cx/cy were measured at 848x480, not the stock
+    // example's original 640x480 - using the wrong resolution here would silently
+    // apply the wrong intrinsics to the actual image content).
+    cfg.enable_stream(RS2_STREAM_INFRARED, 1, 848, 480, RS2_FORMAT_Y8, 60);
+    cfg.enable_stream(RS2_STREAM_INFRARED, 2, 848, 480, RS2_FORMAT_Y8, 60);
     cfg.enable_stream(RS2_STREAM_ACCEL, RS2_FORMAT_MOTION_XYZ32F);
     cfg.enable_stream(RS2_STREAM_GYRO, RS2_FORMAT_MOTION_XYZ32F);
 
@@ -442,6 +446,16 @@ int main(int argc, char **argv) {
         // Clear the previous IMU measurements to load the new ones
         vImuMeas.clear();
     }
+
+    // Stop all threads and (if a trajectory_file_name was given on the command line)
+    // save the tracked trajectory in TUM format (timestamp x y z qx qy qz qw) - the
+    // "trajectory_file_name" argument was already parsed above (see file_name / the
+    // usage message) but was never actually wired to a save call in the stock example.
+    SLAM.Shutdown();
+    if (!file_name.empty()) {
+        SLAM.SaveTrajectoryTUM(file_name);
+        cout << "Trajectory saved to " << file_name << endl;
+    }
     cout << "System shutdown!\n";
 }
 
diff --git a/Examples/Stereo/stereo_euroc.cc b/Examples/Stereo/stereo_euroc.cc
index 1250f2e..b3df31f 100644
--- a/Examples/Stereo/stereo_euroc.cc
+++ b/Examples/Stereo/stereo_euroc.cc
@@ -88,7 +88,9 @@ int main(int argc, char **argv)
     cout.precision(17);
 
     // Create SLAM system. It initializes all system threads and gets ready to process frames.
-    ORB_SLAM3::System SLAM(argv[1],argv[2],ORB_SLAM3::System::STEREO, true);
+    ORB_SLAM3::System SLAM(argv[1],argv[2],ORB_SLAM3::System::STEREO, false);
+    if(std::getenv("ORB_SLAM_LOCALIZATION_ONLY") != nullptr)
+        SLAM.ActivateLocalizationMode();
 
     cv::Mat imLeft, imRight;
     for (seq = 0; seq<num_seq; seq++)
diff --git a/include/Atlas.h b/include/Atlas.h
index e1e2717..8076d76 100644
--- a/include/Atlas.h
+++ b/include/Atlas.h
@@ -139,6 +139,18 @@ public:
 
     long unsigned int GetNumLivedMP();
 
+    // Local addition (not upstream, see orb_capi.cc): a counter bumped by
+    // BOTH CreateNewMap() and clearMap(). GetCurrentMap()->GetId() alone is
+    // NOT a reliable "did the active map's pose origin just change" signal:
+    // clearMap() (used by Tracking::ResetActiveMap(), the path actually
+    // taken by both the "Not enough motion for initializing" and "IMU is
+    // not or recently initialized" resets seen in live testing) clears and
+    // re-initializes the SAME Map object in place, so its mnId never
+    // changes, even though every point/keyframe was just discarded and
+    // tracking restarts from a fresh origin. This counter changes on every
+    // reset that can affect the pose origin, regardless of which path.
+    long unsigned int GetResetGeneration();
+
 protected:
 
     std::set<Map*> mspMaps;
@@ -152,6 +164,9 @@ protected:
 
     unsigned long int mnLastInitKFidMap;
 
+    // Local addition (not upstream) -- see GetResetGeneration()'s doc comment.
+    long unsigned int mnResetGeneration = 0;
+
     Viewer* mpViewer;
     bool mHasViewer;
 
diff --git a/include/System.h b/include/System.h
index 872c86e..26c9b2b 100644
--- a/include/System.h
+++ b/include/System.h
@@ -177,6 +177,23 @@ public:
     std::vector<MapPoint*> GetTrackedMapPoints();
     std::vector<cv::KeyPoint> GetTrackedKeyPointsUn();
 
+    // Local addition (not upstream, see orb_capi.cc): a counter that changes
+    // every time ORB-SLAM3 resets in a way that can move the active map's
+    // pose origin -- e.g. after "Not enough motion for initializing.
+    // Reseting..." or "IMU is not or recently initialized. Reseting active
+    // map..." during Stereo-Inertial tracking -- without that being visible
+    // through GetTrackingState() (which reports OK either way once tracking
+    // resumes on the reset map). Comparing this value across calls is how a
+    // caller detects "the poses before and after this frame are NOT in the
+    // same coordinate frame, don't treat them as continuous". Backed by
+    // Atlas::GetResetGeneration() rather than the active map's own ID
+    // (Map::GetId()) because live testing showed the common reset path
+    // (Tracking::ResetActiveMap(), Atlas::clearMap()) clears and
+    // re-initializes the SAME Map object in place -- its ID never changes
+    // even though the origin does -- see Atlas.h's doc comment for the
+    // full story.
+    long unsigned int GetMapEpoch();
+
     // For debugging
     double GetTimeFromIMUInit();
     bool isLost();
diff --git a/src/Atlas.cc b/src/Atlas.cc
index 7bdea06..c413415 100644
--- a/src/Atlas.cc
+++ b/src/Atlas.cc
@@ -74,6 +74,7 @@ void Atlas::CreateNewMap()
     mpCurrentMap = new Map(mnLastInitKFidMap);
     mpCurrentMap->SetCurrentMap();
     mspMaps.insert(mpCurrentMap);
+    mnResetGeneration++;  // see GetResetGeneration()'s doc comment in Atlas.h
 }
 
 void Atlas::ChangeMap(Map* pMap)
@@ -231,6 +232,13 @@ void Atlas::clearMap()
 {
     unique_lock<mutex> lock(mMutexAtlas);
     mpCurrentMap->clear();
+    mnResetGeneration++;  // see GetResetGeneration()'s doc comment in Atlas.h
+}
+
+long unsigned int Atlas::GetResetGeneration()
+{
+    unique_lock<mutex> lock(mMutexAtlas);
+    return mnResetGeneration;
 }
 
 void Atlas::clearAtlas()
diff --git a/src/Optimizer.cc b/src/Optimizer.cc
index b785be3..9f16283 100644
--- a/src/Optimizer.cc
+++ b/src/Optimizer.cc
@@ -2392,7 +2392,8 @@ void Optimizer::LocalInertialBA(KeyFrame *pKF, bool *pbStopFlag, Map *pMap, int&
         opt_it=4;
     }
     const int Nd = std::min((int)pCurrentMap->KeyFramesInMap()-2,maxOpt);
-    const unsigned long maxKFid = pKF->mnId;
+    // maxKFid is computed further down, once the keyframe lists exist -- same
+    // vertex-id collision as MergeInertialBA, see the comment there.
 
     vector<KeyFrame*> vpOptimizableKFs;
     const vector<KeyFrame*> vpNeighsKFs = pKF->GetVectorCovisibleKeyFrames();
@@ -2524,6 +2525,25 @@ void Optimizer::LocalInertialBA(KeyFrame *pKF, bool *pbStopFlag, Map *pMap, int&
     }
 
 
+    // Local change (not upstream): the largest keyframe id in this
+    // optimization, not pKF's own -- see MergeInertialBA for the full
+    // explanation of the packed vertex-id layout and what goes wrong.
+    //
+    // This one matters more often than the merge case: LocalInertialBA runs
+    // on essentially every keyframe (LocalMapping::Run), not just when two
+    // maps join. Cold-starting a map makes ids ascend with pKF, so upstream's
+    // value happens to be the maximum and nothing collides -- which is why
+    // this never showed up before loading an atlas. Once a saved map is
+    // loaded, its keyframe ids and this session's interleave and pKF is
+    // routinely not the newest.
+    unsigned long maxKFid = pKF->mnId;
+    for(KeyFrame* pKFi : vpOptimizableKFs)
+        if(pKFi && pKFi->mnId > maxKFid) maxKFid = pKFi->mnId;
+    for(KeyFrame* pKFi : lpOptVisKFs)
+        if(pKFi && pKFi->mnId > maxKFid) maxKFid = pKFi->mnId;
+    for(KeyFrame* pKFi : lFixedKeyFrames)
+        if(pKFi && pKFi->mnId > maxKFid) maxKFid = pKFi->mnId;
+
     // Set Local temporal KeyFrame vertices
     N=vpOptimizableKFs.size();
     for(int i=0; i<N; i++)
@@ -3948,7 +3968,9 @@ void Optimizer::LocalBundleAdjustment(KeyFrame* pMainKF,vector<KeyFrame*> vpAdju
 void Optimizer::MergeInertialBA(KeyFrame* pCurrKF, KeyFrame* pMergeKF, bool *pbStopFlag, Map *pMap, LoopClosing::KeyFrameAndPose &corrPoses)
 {
     const int Nd = 6;
-    const unsigned long maxKFid = pCurrKF->mnId;
+    // maxKFid is computed further down, once the keyframe lists exist -- see
+    // the comment there. Upstream sets it to pCurrKF->mnId right here, which
+    // is not the maximum and collides vertex ids.
 
     vector<KeyFrame*> vpOptimizableKFs;
     vpOptimizableKFs.reserve(2*Nd);
@@ -4100,6 +4122,39 @@ void Optimizer::MergeInertialBA(KeyFrame* pCurrKF, KeyFrame* pMergeKF, bool *pbS
     optimizer.setAlgorithm(solver);
     optimizer.setVerbose(false);
 
+    // Local change (not upstream): maxKFid must be the LARGEST keyframe id
+    // taking part in this optimization, because the vertex-id layout below
+    // is packed against it:
+    //
+    //     pose vertex          id = pKFi->mnId
+    //     velocity/bias        id = maxKFid + 3*pKFi->mnId + {1,2,3}
+    //     map point            id = maxKFid*5 + pMP->mnId + 1
+    //
+    // The pose block only stays below the IMU block while every id is <=
+    // maxKFid. Upstream initialises it to pCurrKF->mnId -- the CURRENT
+    // keyframe, not the maximum -- so any keyframe with a higher id lands its
+    // pose vertex inside the IMU range, and g2o rejects the duplicate:
+    // "addVertex: FATAL, a vertex with ID N has already been registered".
+    // Rejected vertices are silently left out of the graph, and edges then
+    // resolve that id to whichever vertex got there first -- of the wrong
+    // type.
+    //
+    // Merging a loaded atlas is what exposes this here. The two maps' ids
+    // come from different sessions and interleave, so the "current" keyframe
+    // is routinely not the newest. Observed live as 20 rejected vertices in
+    // two clusters (438-442 and 1757-1771) during a single merge.
+    //
+    // Computing it properly also retires the `if (pKFi->mnId > maxKFid)
+    // continue;` guard in the observation loop below, which exists only to
+    // paper over this overflow and drops otherwise-valid observations.
+    unsigned long maxKFid = pCurrKF->mnId;
+    for(KeyFrame* pKFi : vpOptimizableKFs)
+        if(pKFi && pKFi->mnId > maxKFid) maxKFid = pKFi->mnId;
+    for(KeyFrame* pKFi : vpOptimizableCovKFs)
+        if(pKFi && pKFi->mnId > maxKFid) maxKFid = pKFi->mnId;
+    for(KeyFrame* pKFi : lFixedKeyFrames)
+        if(pKFi && pKFi->mnId > maxKFid) maxKFid = pKFi->mnId;
+
     // Set Local KeyFrame vertices
     N=vpOptimizableKFs.size();
     for(int i=0; i<N; i++)
diff --git a/src/Settings.cc b/src/Settings.cc
index 93a50be..b26afad 100644
--- a/src/Settings.cc
+++ b/src/Settings.cc
@@ -550,7 +550,10 @@ namespace ORB_SLAM3 {
             output << " ]" << endl;
         }
 
-        if(settings.sensor_ == System::STEREO || settings.sensor_ == System::IMU_STEREO){
+        // A Rectified stereo configuration intentionally has one shared
+        // pinhole calibration plus Stereo.b; originalCalib2_ is null.
+        if((settings.sensor_ == System::STEREO || settings.sensor_ == System::IMU_STEREO) &&
+           settings.cameraType_ != Settings::Rectified){
             output << "\t-Camera 2 parameters (";
             if(settings.cameraType_ == Settings::PinHole || settings.cameraType_ ==  Settings::Rectified){
                 output << "Pinhole";
diff --git a/src/System.cc b/src/System.cc
index 60d9c51..e999bf8 100644
--- a/src/System.cc
+++ b/src/System.cc
@@ -21,6 +21,7 @@
 #include "System.h"
 #include "Converter.h"
 #include <thread>
+#include <chrono>
 #include <pangolin/pangolin.h>
 #include <iomanip>
 #include <openssl/md5.h>
@@ -530,20 +531,50 @@ void System::Shutdown()
             usleep(5000);
     }*/
 
-    // Wait until all thread have effectively stopped
-    /*while(!mpLocalMapper->isFinished() || !mpLoopCloser->isFinished() || mpLoopCloser->isRunningGBA())
+    // Local change (not upstream): actually wait for the mapping and
+    // loop-closing threads to stop. Upstream requests them to finish and
+    // then returns immediately -- the wait that used to be here is the
+    // commented-out block this replaces.
+    //
+    // Everything after this point walks the atlas: SaveAtlas() below, and
+    // SaveTrajectoryTUM()/SaveKeyFrameTrajectory*(), which callers are told
+    // to invoke after Shutdown(). Without the wait, those run while
+    // LocalMapping is still inserting and culling keyframes and LoopClosing
+    // is still merging maps -- reading containers another thread is
+    // mutating. Neither thread is ever joined either (mptLocalMapping /
+    // mptLoopClosing are new'd and never deleted, and System has no
+    // destructor), so they can also outlive the objects they point into.
+    //
+    // Seen live on this project, three times before this change: heap
+    // corruption ("free(): corrupted unsorted chunks") immediately after an
+    // atlas save, and "FATAL: exception not rethrown" + SIGABRT at process
+    // exit after a recording. In every case the output file was already
+    // complete, which is exactly what a race that only bites during teardown
+    // looks like.
+    //
+    // Bounded rather than unconditional, because the reason to remove it is
+    // real: LoopClosing::isRunningGBA() can stay true for a long time on a
+    // large map, and hanging on shutdown would be a worse failure than an
+    // inconsistent save. On timeout, say so loudly and continue -- upstream's
+    // own commented-out version had the same "break anyway" escape.
     {
-        if(!mpLocalMapper->isFinished())
-            cout << "mpLocalMapper is not finished" << endl;*/
-        /*if(!mpLoopCloser->isFinished())
-            cout << "mpLoopCloser is not finished" << endl;
-        if(mpLoopCloser->isRunningGBA()){
-            cout << "mpLoopCloser is running GBA" << endl;
-            cout << "break anyway..." << endl;
-            break;
-        }*/
-        /*usleep(5000);
-    }*/
+        const std::chrono::steady_clock::time_point deadline =
+            std::chrono::steady_clock::now() + std::chrono::seconds(20);
+        while(!mpLocalMapper->isFinished() || !mpLoopCloser->isFinished() || mpLoopCloser->isRunningGBA())
+        {
+            if(std::chrono::steady_clock::now() > deadline)
+            {
+                cerr << "Shutdown: mapping/loop-closing threads still running after 20s"
+                     << " (LocalMapping finished=" << mpLocalMapper->isFinished()
+                     << ", LoopClosing finished=" << mpLoopCloser->isFinished()
+                     << ", GBA running=" << mpLoopCloser->isRunningGBA()
+                     << ") -- continuing anyway; anything saved below may be inconsistent."
+                     << endl;
+                break;
+            }
+            usleep(5000);
+        }
+    }
 
     if(!mStrSaveAtlasToFile.empty())
     {
@@ -1324,6 +1355,14 @@ int System::GetTrackingState()
     return mTrackingState;
 }
 
+long unsigned int System::GetMapEpoch()
+{
+    // Atlas::GetResetGeneration() takes its own internal mutex -- safe to
+    // call any time, no separate locking needed here (see System.h's and
+    // Atlas.h's comments on this).
+    return mpAtlas->GetResetGeneration();
+}
+
 vector<MapPoint*> System::GetTrackedMapPoints()
 {
     unique_lock<mutex> lock(mMutexState);
